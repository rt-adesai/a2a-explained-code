# Copy to .env and fill in. Never commit .env.
# The sample reads these names (its own choice); any OpenAI-compatible endpoint works.
model_source=openai
TOOL_LLM_URL=https://openrouter.ai/api/v1
TOOL_LLM_NAME=anthropic/claude-sonnet-5
API_KEY=
